## This is the alternative of how you manipulate processes in Task Manager, but doing it in Powershell

```powershell
Get-Process
Get-Process -Name chrome
Stop-Process -Name chrome
```

Use this to find process associated with PID <br>
Useful after using 'netstat -ano' to identify what program is using a port
```powershell
Get-Process -Id 1234
```
